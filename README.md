# D'Madame — site

Site institucional da D'Madame, moda feminina em Tatuí/SP.

- `index.html` — página única (HTML, CSS e JS inline; smooth scroll via Lenis)
- `assets/` — imagens
- `serve.js` — servidor local: `node serve.js` → http://localhost:5173
